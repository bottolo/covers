# Covers

Local development uses standard Docker and Docker Compose.

## Run Locally

```bash
docker compose up --build
```

Open [http://localhost:5173](http://localhost:5173).

## Stop

```bash
docker compose down
```
